name: Hello World Training Workflow ( ͡° ͜ʖ ͡°)

on:
  workflow_dispatch:

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Greet the User
        run: echo "8=======>"
      - run: ls -l
      - uses: actions/checkout@v2
      - run: ls -l
